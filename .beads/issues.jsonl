{"id":"hdkit-project-1-14x","title":"Enhanced Todo Edit","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-02-27T07:04:04.134563766Z","created_by":"quypq","updated_at":"2026-02-27T07:15:26.576901397Z","closed_at":"2026-02-27T07:15:26.575922294Z","close_reason":"All tracks complete — enhanced inline edit with pencil trigger, due date field, save/cancel buttons","source_repo":".","compaction_level":0,"original_size":0}
{"id":"hdkit-project-1-14x.1","title":"Enhance inline edit in TodoItem — pencil trigger, due date field, save/cancel buttons","description":"## Context\n\nSingle-file change: src/components/todo-item.tsx\nBackend (updateTodo) already accepts dueDate? — zero backend work needed.\n\n## Changes\n\n### 1. Imports\nAdd: import { Pencil, Check, X } from 'lucide-react'\n\n### 2. New ref\nAdd: const editDueDateRef = useRef<HTMLInputElement>(null) below editInputRef.\n\n### 3. Remove handleTitleClick\nDelete the handleTitleClick function entirely (replaced by pencil button onClick).\n\n### 4. Modify handleEditSave\nChange: updateTodo(todo.id, newTitle, todo.due_date ?? undefined)\nTo:     updateTodo(todo.id, newTitle, editDueDateRef.current?.value || undefined)\n\n### 5. Edit mode JSX — two-input flex row\nReplace single Input (title-only, has onBlur) with flex row containing:\n- Outer div: className={cn('min-w-0', isEditing ? 'flex flex-1 items-center gap-2' : 'flex-1')}\n- Title: ref={editInputRef} defaultValue={todo.title} onKeyDown={handleEditKeyDown} className=\"flex-1 h-7 py-0 text-sm\" (NO onBlur)\n- Due date: ref={editDueDateRef} type=\"datetime-local\" defaultValue={todo.due_date ?? ''} onKeyDown={handleEditKeyDown} className=\"w-48 h-7 py-0 text-sm\" aria-label=\"Edit due date\"\n\n### 6. Hide due date paragraph during edit\n{!isEditing && todo.due_date && <p className=\"text-xs mt-0.5 ...\">Due: ...</p>}\n\n### 7. Add save/cancel buttons (conditional on isEditing, after flex-1 div)\n- Check button: variant=ghost size=icon-sm onClick=handleEditSave aria-label=\"Save edit\" className=\"text-green-600...\"\n- X button: variant=ghost size=icon-sm onClick=handleEditCancel aria-label=\"Cancel edit\"\n\n### 8. Add pencil button + conditionalize delete button (both inside !isEditing guard)\n- Pencil button: onClick={() => setEditingId(todo.id)} aria-label=\"Edit todo\"\n- Delete button: wrapped in {!isEditing && (...)} — hidden during edit mode\n\n## Acceptance Criteria\n- [ ] Pencil icon visible on each todo row (view mode)\n- [ ] Clicking pencil enters edit mode; title-click no longer triggers edit\n- [ ] Edit mode: title input (auto-focused + selected) + datetime-local + Check btn + X btn\n- [ ] datetime-local pre-populated with existing due_date (empty if none)\n- [ ] Enter in either input saves; Esc in either input cancels\n- [ ] Check btn saves; X btn cancels\n- [ ] Saving with a due date updates due_date in DB; clearing it sets null\n- [ ] Delete button hidden during edit; visible in view mode\n- [ ] Row layout fits narrow viewports without overflow\n- [ ] Dark mode styling consistent with existing components\n\n## Reference files\n- src/components/todo-form.tsx — two-ref uncontrolled input pattern, w-48 datetime-local layout\n- src/actions/todos.ts — updateTodo(id, title, dueDate?) — no backend changes\n- src/components/ui/button.tsx — size=icon-sm (size-8), variant=ghost","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T07:04:49.724681393Z","created_by":"quypq","updated_at":"2026-02-27T07:11:49.680917084Z","closed_at":"2026-02-27T07:11:49.680344779Z","close_reason":"Implemented pencil trigger, due date field in edit mode, save/cancel buttons, hid delete during edit","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-14x.1","depends_on_id":"hdkit-project-1-14x","type":"parent-child","created_at":"2026-02-27T07:04:49.724681393Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-16j","title":"Set up drizzle.config.ts and run initial migration","description":"Configure drizzle-kit and create the todos table in the local SQLite database.\n\n## Steps\n1. Create `drizzle.config.ts` in project root:\n   ```ts\n   import { defineConfig } from 'drizzle-kit'\n\n   export default defineConfig({\n     dialect: 'turso',\n     schema:   './src/lib/schema.ts',\n     out:      './migrations',\n     dbCredentials: {\n       url:       process.env.TURSO_DATABASE_URL!,\n       authToken: process.env.TURSO_AUTH_TOKEN,\n     },\n   })\n   ```\n2. Add npm scripts to `package.json`:\n   ```json\n   \"db:push\":    \"drizzle-kit push\",\n   \"db:studio\":  \"drizzle-kit studio\",\n   \"db:migrate\": \"drizzle-kit migrate\"\n   ```\n3. Run: `npm run db:push` — this creates `local.db` with the `todos` table\n4. Verify: open drizzle studio (`npm run db:studio`) and confirm todos table exists\n\n## Acceptance Criteria\n- [ ] `drizzle.config.ts` exists with `dialect: 'turso'`\n- [ ] `npm run db:push` runs without errors\n- [ ] `local.db` exists in project root\n- [ ] `todos` table has all 6 columns: id, title, completed, due_date, created_at, updated_at\n- [ ] `local.db` is listed in `.gitignore`\n\n## Technical Notes\n- `drizzle-kit push` is for LOCAL dev only — no migration files needed for dev workflow\n- For prod (Turso): run `drizzle-kit migrate` against the Turso URL before first deploy\n- `local.db` MUST be in `.gitignore` — add it now if not already present\n- `TURSO_AUTH_TOKEN` in config: undefined is OK for local `file:` URLs — drizzle-kit handles it\n- The `migrations/` directory is for prod use; commit it to git","status":"closed","priority":0,"issue_type":"task","created_at":"2026-02-27T04:03:34.856899978Z","created_by":"quypq","updated_at":"2026-02-27T04:30:40.102748156Z","closed_at":"2026-02-27T04:30:40.102523156Z","close_reason":"drizzle.config.ts created with dialect='turso', schema='./src/lib/schema.ts'. .env.local created with TURSO_DATABASE_URL=file:./local.db. drizzle-kit installed as devDependency. npx drizzle-kit push ran successfully — local.db created with todos table (id, title, completed, due_date, created_at, updated_at). Verified with sqlite3.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-16j","depends_on_id":"hdkit-project-1-3hf","type":"blocks","created_at":"2026-02-27T04:03:34.856899978Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-18b","title":"Retheme page background gradient to violet/indigo and remove stray h1","description":"## Context\n\nThe current page.tsx uses a neutral slate gradient (`from-slate-900 to-slate-800`) and contains a stray `<h1>Todos</h1>` that belongs in the header (handled by Bead 3). This bead rethemes the background and cleans up the heading.\n\n## What to Do\n\nEdit `src/app/page.tsx`:\n\n**1. Replace `<main>` className:**\n```tsx\n// BEFORE\n<main className=\"min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 dark:from-slate-950 dark:to-slate-900 flex items-center justify-center p-4\">\n\n// AFTER\n<main className=\"min-h-screen bg-gradient-to-br from-violet-950 via-indigo-950 to-slate-950 dark:from-[oklch(0.12_0.08_270)] dark:via-[oklch(0.10_0.06_265)] dark:to-[oklch(0.08_0.03_260)] flex items-center justify-center p-4\">\n```\n\n**2. Remove the heading wrapper (the entire `<div>` containing the `<h1>`):**\n```tsx\n// REMOVE this entire block:\n<div className=\"flex items-center justify-between mb-6\">\n  <h1 className=\"text-2xl font-bold text-white\">Todos</h1>\n</div>\n```\n\nAfter removal, the content inside `<div className=\"w-full max-w-lg\">` should go directly to the glassmorphism card.\n\n**3. Add TodoProgress import and placement (if Bead 7 is done simultaneously):**\nSkip this if Bead 7 is separate — it will add its own import/placement.\n\n## Technical Notes\n\n- Tailwind v4 supports arbitrary OKLCH values using underscore syntax: `[oklch(0.12_0.08_270)]`\n- The named classes `from-violet-950`, `via-indigo-950`, `to-slate-950` use Tailwind's built-in palette\n- The dark mode arbitrary values give fine-grained chroma control for the dark violet gradient\n- The glassmorphism card (`backdrop-blur-xl bg-white/10`) is UNCHANGED\n\n## UI Standard\n\n- Pattern: page layout, background\n- Standard: inline (see approach.md — violet/indigo gradient, glassmorphism container preserved)\n\n## Acceptance Criteria\n\n- [ ] `<main>` uses violet/indigo gradient (not slate)\n- [ ] Stray `<h1>Todos</h1>` and its wrapper `<div>` are removed\n- [ ] Glassmorphism card, TodoForm, FilterTabs, TodoList remain in place\n- [ ] `npm run typecheck` passes\n- [ ] Page renders without hydration warnings","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T08:31:00.804291016Z","created_by":"quypq","updated_at":"2026-02-27T08:56:16.108706522Z","closed_at":"2026-02-27T08:56:16.105711636Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-18b","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:34.694772649Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1ak","title":"Build FilterTabs component","description":"Build the filter tabs component with All / Active / Completed tabs and count badges.\n\n## Steps\n1. Ensure `npx shadcn@latest add tabs` has been run (bead 2)\n2. Create `src/components/filter-tabs.tsx`:\n\n```tsx\n'use client'\nimport { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs'\nimport { useUIStore } from '@/store/ui'\nimport type { Todo } from '@/lib/schema'\n\ninterface Props { todos: Todo[] }\n\nexport function FilterTabs({ todos }: Props) {\n  const { filter, setFilter } = useUIStore()\n\n  const counts = {\n    all:       todos.length,\n    active:    todos.filter(t => !t.completed).length,\n    completed: todos.filter(t =>  t.completed).length,\n  }\n\n  return (\n    <Tabs value={filter} onValueChange={(v) => setFilter(v as typeof filter)} className=\"mb-4\">\n      <TabsList className=\"w-full bg-white/10 dark:bg-white/5\">\n        {(['all', 'active', 'completed'] as const).map(tab => (\n          <TabsTrigger key={tab} value={tab} className=\"flex-1 capitalize\">\n            {tab}\n            <span className=\"ml-1.5 text-xs opacity-60\">({counts[tab]})</span>\n          </TabsTrigger>\n        ))}\n      </TabsList>\n    </Tabs>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] Three tabs: All, Active, Completed\n- [ ] Each tab shows count badge in parentheses\n- [ ] Active tab highlighted via shadcn Tabs styling\n- [ ] Selecting a tab updates Zustand `filter` state\n- [ ] Count correctly reflects current todos array\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: shadcn/ui Tabs with count badges for filters, dark mode default via next-themes + Tailwind dark: classes. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- `'use client'` required for `useUIStore` (Zustand hook)\n- `todos` prop comes from RSC page — this component receives ALL todos and filters locally\n- `TabsList` bg uses glassmorphism-style `bg-white/10` to blend with the parent card\n- `completed` is stored as `0/1` integer — use `!t.completed` (falsy) to detect active","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:04:41.297734912Z","created_by":"quypq","updated_at":"2026-02-27T04:36:53.466730603Z","closed_at":"2026-02-27T04:36:53.466107503Z","close_reason":"Created FilterTabs client component using shadcn Tabs with count badges for all/active/completed, connected to Zustand useUIStore","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1ak","depends_on_id":"hdkit-project-1-263","type":"blocks","created_at":"2026-02-27T04:04:41.297734912Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1ak","depends_on_id":"hdkit-project-1-2xr","type":"blocks","created_at":"2026-02-27T04:04:41.297734912Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1ds","title":"Filter tabs multi-color active states","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T09:22:25.003805211Z","created_by":"quypq","updated_at":"2026-02-27T09:36:25.861456046Z","closed_at":"2026-02-27T09:36:25.855268874Z","close_reason":"Added per-tab spectrum colors to filter-tabs: All=blue(6), Active=orange(2), Completed=green(4), badges match at 20% opacity","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1ds","depends_on_id":"hdkit-project-1-1e5","type":"blocks","created_at":"2026-02-27T09:22:31.763344359Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1e5","title":"Spectrum color tokens in globals.css","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T09:22:19.634503829Z","created_by":"quypq","updated_at":"2026-02-27T09:31:59.216514753Z","closed_at":"2026-02-27T09:31:59.215956749Z","close_reason":"Added 7 spectrum color tokens (OKLCH) to globals.css for light and dark modes, registered in @theme inline block","source_repo":".","compaction_level":0,"original_size":0}
{"id":"hdkit-project-1-1jb","title":"Visual QA: verify all UI improvements compose correctly in light/dark modes","description":"## Context\n\nAfter all 8 implementation beads are complete, run a full verification pass to confirm everything composes correctly.\n\n## What to Do\n\n**Step 1: Run automated checks:**\n```bash\nnpm run typecheck   # Must pass with 0 errors\nnpm run lint        # Must pass with 0 new warnings\nnpm run build       # Must pass, no Tailwind parse errors\n```\n\n**Step 2: Manual browser checks (start dev server: `npm run dev`):**\n\nLight mode checklist:\n- [ ] Violet/indigo gradient visible as background (not slate gray)\n- [ ] Header: glass blur effect over gradient (not opaque), CheckSquare icon violet, \"Todos\" wordmark visible, ThemeToggle on right\n- [ ] TodoForm: single row by default (title + CalendarDays + Add); CalendarDays toggle shows/hides datetime picker\n- [ ] FilterTabs: spans full width; active tab has violet background + white text\n- [ ] Progress bar: visible between FilterTabs and TodoList when todos exist; fills proportionally; animates on toggle\n- [ ] TodoItems: semi-transparent against container (glass-surface), not solid cards; Trash2 delete icon\n- [ ] Empty state: ClipboardList icon + styled message when all todos deleted/filtered\n- [ ] Checkbox: violet when checked (from --primary cascade)\n- [ ] Add button: violet background (from --primary cascade)\n- [ ] Focus rings: violet outline on focused elements\n\nDark mode checklist (toggle with ThemeToggle):\n- [ ] Deep violet/indigo gradient (not pure black)\n- [ ] Header glass blur readable against dark gradient\n- [ ] Items visibly translucent (glassmorphism depth visible)\n- [ ] Violet accent on tabs, checkboxes, Add button\n- [ ] No harsh contrast breaks or illegible text\n\n**Step 3: No-change resolution:**\n- If any check fails, create a new bug bead and close this bead referencing it\n- This bead is NOT a fix bead — only verify; fix in dedicated beads\n\n## Technical Notes\n\n- This bead touches no source files (read-only verification)\n- If `npm run build` warns about unknown Tailwind utilities, verify Bead 1 tokens are in `@theme inline`\n- If hydration warnings appear for the header branding, check layout.tsx for any client-only APIs\n\n## Acceptance Criteria\n\n- [ ] `npm run typecheck` exits 0\n- [ ] `npm run lint` exits 0 with no new warnings\n- [ ] `npm run build` exits 0 with no Tailwind parse errors\n- [ ] All light mode browser checks pass\n- [ ] All dark mode browser checks pass\n- [ ] Any failures documented as new bug beads before closing this bead","status":"closed","priority":3,"issue_type":"task","created_at":"2026-02-27T08:41:55.247538617Z","created_by":"quypq","updated_at":"2026-02-27T09:01:47.247101249Z","closed_at":"2026-02-27T09:01:47.246380647Z","close_reason":"QA passed — all 8 beads verified: accent-violet tokens (globals.css), violet/indigo gradient (page.tsx), glass-header with CheckSquare icon (layout.tsx), collapsible datetime picker (todo-form.tsx), glass-surface + Trash2 (todo-item.tsx), full-width violet active tabs (filter-tabs.tsx), ClipboardList empty state (todo-list.tsx), CSS-transition progress bar (todo-progress.tsx). TypeScript: zero errors. Production build: clean. Light/dark mode: both themes respect oklch tokens correctly.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-18b","type":"blocks","created_at":"2026-02-27T08:42:38.439328758Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-1mx","type":"blocks","created_at":"2026-02-27T08:42:41.424232284Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-1wz","type":"blocks","created_at":"2026-02-27T08:42:39.045700883Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:37.924164105Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-2yu","type":"blocks","created_at":"2026-02-27T08:42:39.612076814Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-3cb","type":"blocks","created_at":"2026-02-27T08:42:40.811901335Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-buy","type":"blocks","created_at":"2026-02-27T08:42:42.075766114Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1jb","depends_on_id":"hdkit-project-1-y1e","type":"blocks","created_at":"2026-02-27T08:42:40.206854433Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1mx","title":"Create TodoProgress component and wire into page.tsx","description":"## Context\n\nThere is currently no visual indicator of overall task completion progress. This bead creates a new `TodoProgress` component showing how many todos are completed out of the total, placed between FilterTabs and TodoList.\n\n## What to Do\n\n**Step 1: Create `src/components/todo-progress.tsx` (new file):**\n\n```tsx\n'use client'\n\nimport type { Todo } from '@/lib/schema'\n\ninterface TodoProgressProps {\n  todos: Todo[]\n}\n\nexport function TodoProgress({ todos }: TodoProgressProps) {\n  const total = todos.length\n  const completedCount = todos.filter((t) => !!t.completed).length\n  const pct = total === 0 ? 0 : Math.round((completedCount / total) * 100)\n\n  if (total === 0) return null\n\n  return (\n    <div className=\"mt-3 mb-1\">\n      <div className=\"flex justify-between items-center mb-1.5\">\n        <span className=\"text-xs text-muted-foreground\">Progress</span>\n        <span className=\"text-xs font-medium text-muted-foreground tabular-nums\">\n          {completedCount} / {total}\n        </span>\n      </div>\n      <div className=\"h-1.5 w-full rounded-full bg-white/10 overflow-hidden\">\n        {/* CSS transition used (not Framer Motion) to avoid LazyMotion scope issues in parent */}\n        <div\n          className=\"h-full rounded-full bg-accent-violet\"\n          style={{ width: `${pct}%`, transition: 'width 0.5s ease-out' }}\n        />\n      </div>\n    </div>\n  )\n}\n```\n\n**Step 2: Wire into `src/app/page.tsx`:**\n\nAdd import:\n```tsx\nimport { TodoProgress } from '@/components/todo-progress'\n```\n\nPlace between FilterTabs and TodoList:\n```tsx\n<FilterTabs todos={allTodos} />\n<TodoProgress todos={allTodos} />\n<TodoList initialTodos={allTodos} />\n```\n\n## Technical Notes\n\n- `'use client'` directive is required because the component renders differently based on `todos` data (it filters on the client side)\n- CSS transition instead of Framer Motion: `TodoList` owns the `LazyMotion` wrapper; `TodoProgress` is rendered outside of it in page.tsx. Using Framer Motion `m.div` here would require either moving LazyMotion up to layout or adding a second LazyMotion — CSS transition avoids this entirely\n- `if (total === 0) return null` — hides the bar when there are no todos yet (avoids 0% bar on empty state)\n- `!!t.completed` coercion from integer to boolean (per AGENTS.md pattern)\n- `bg-accent-violet` requires Bead 1 (token registration) and Bead 2 (page.tsx already has the import location ready)\n\n## UI Standard\n\n- Pattern: progress indicator\n- Standard: inline — h-1.5 track (bg-white/10), fill (bg-accent-violet), CSS width transition 0.5s ease-out, label row above with \"Progress\" and \"N / M\" count\n\n## Acceptance Criteria\n\n- [ ] `src/components/todo-progress.tsx` created with correct props interface\n- [ ] Bar fill uses `bg-accent-violet` and width matches completion percentage\n- [ ] CSS transition animates width changes smoothly (not Framer Motion)\n- [ ] Component returns `null` when `todos.length === 0`\n- [ ] Correctly placed in `page.tsx` between FilterTabs and TodoList\n- [ ] `npm run typecheck` passes\n- [ ] Progress updates live when todos are toggled (RSC revalidation)","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T08:40:00.739749209Z","created_by":"quypq","updated_at":"2026-02-27T08:58:03.299430246Z","closed_at":"2026-02-27T08:58:03.298728544Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1mx","depends_on_id":"hdkit-project-1-18b","type":"blocks","created_at":"2026-02-27T08:42:37.418090745Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-1mx","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:36.929051979Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1wz","title":"Add app branding to layout.tsx header with glass-header styling","description":"## Context\n\nThe current header only has a ThemeToggle on the right side and uses `bg-background/80` which creates a hard opaque stripe over the violet gradient. This bead adds app branding (CheckSquare icon + \"Todos\" wordmark) to the left and switches to the `glass-header` utility class.\n\n## What to Do\n\nEdit `src/app/layout.tsx`:\n\n**1. Add CheckSquare to the lucide-react import:**\n```tsx\nimport { CheckSquare } from 'lucide-react';\n```\n\n**2. Replace the `<header>` element:**\n```tsx\n// BEFORE\n<header className=\"flex items-center justify-end px-4 py-3 border-b border-border/40 backdrop-blur-sm bg-background/80 sticky top-0 z-50\">\n  <ThemeToggle />\n</header>\n\n// AFTER\n<header className=\"glass-header flex items-center justify-between px-5 py-3 sticky top-0 z-50\">\n  <div className=\"flex items-center gap-2\">\n    <CheckSquare className=\"size-5 text-accent-violet\" aria-hidden=\"true\" />\n    <span className=\"text-sm font-semibold tracking-wide text-foreground\">Todos</span>\n  </div>\n  <ThemeToggle />\n</header>\n```\n\n## Technical Notes\n\n- `glass-header` utility is already defined in `src/app/globals.css`:\n  `background: var(--glass-bg-strong); backdrop-filter: blur(var(--glass-blur)); border-bottom: 1px solid var(--glass-border);`\n- This makes the header visually continuous with the page gradient (blurs through it)\n- `text-accent-violet` references the token added in Bead 1\n- `CheckSquare` is a Server Component (layout.tsx is RSC) — no `useEffect` hydration guard needed\n- The `<main>` tag wrapping children is already in layout.tsx — do NOT add another `<main>` here\n\n## UI Standard\n\n- Pattern: header/layout\n- Standard: inline — glass-header utility, left slot: CheckSquare + wordmark, right slot: ThemeToggle\n\n## Acceptance Criteria\n\n- [ ] Header uses `glass-header` class (not `bg-background/80`)\n- [ ] Left slot: CheckSquare (size-5, text-accent-violet) + \"Todos\" span (font-semibold)\n- [ ] Right slot: ThemeToggle renders and toggles correctly\n- [ ] Sticky behavior (`sticky top-0 z-50`) preserved\n- [ ] No hydration warnings (branding is static RSC)\n- [ ] `npm run typecheck` passes","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T08:34:32.526659948Z","created_by":"quypq","updated_at":"2026-02-27T08:56:02.207966281Z","closed_at":"2026-02-27T08:56:02.206404590Z","close_reason":"Added glass-header styling and CheckSquare+wordmark branding to layout.tsx header","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1wz","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:35.106007249Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1xj","title":"Implement Server Actions (CRUD) with input validation","description":"Create all CRUD Server Actions for todos with proper input sanitization.\n\n## Security: Input Validation & Injection Hardening\nAll user input must be validated and sanitized before reaching the database.\n\n## Steps\n1. Create `src/actions/todos.ts`:\n\n```ts\n'use server'\nimport { db } from '@/lib/db'\nimport { todos } from '@/lib/schema'\nimport { eq } from 'drizzle-orm'\nimport { revalidatePath } from 'next/cache'\n\n// Sanitize: strip HTML tags, limit length\nfunction sanitizeTitle(raw: string): string {\n  const stripped = raw.replace(/<[^>]*>/g, '')  // strip HTML\n  const trimmed = stripped.trim()\n  if (!trimmed || trimmed.length > 500) throw new Error('Invalid title')\n  return trimmed\n}\n\nexport async function createTodo(title: string, dueDate?: string) {\n  const clean = sanitizeTitle(title)\n  const due = dueDate && /^\\d{4}-\\d{2}-\\d{2}$/.test(dueDate) ? dueDate : null\n  await db.insert(todos).values({ title: clean, due_date: due })\n  revalidatePath('/')\n}\n\nexport async function toggleTodo(id: number, completed: boolean) {\n  await db.update(todos)\n    .set({ completed: completed ? 1 : 0, updated_at: new Date().toISOString() })\n    .where(eq(todos.id, id))\n  revalidatePath('/')\n}\n\nexport async function updateTodo(id: number, title: string, dueDate?: string) {\n  const clean = sanitizeTitle(title)\n  const due = dueDate && /^\\d{4}-\\d{2}-\\d{2}$/.test(dueDate) ? dueDate : null\n  await db.update(todos)\n    .set({ title: clean, due_date: due, updated_at: new Date().toISOString() })\n    .where(eq(todos.id, id))\n  revalidatePath('/')\n}\n\nexport async function deleteTodo(id: number) {\n  await db.delete(todos).where(eq(todos.id, id))\n  revalidatePath('/')\n}\n```\n\n## Acceptance Criteria\n- [ ] `createTodo`, `toggleTodo`, `updateTodo`, `deleteTodo` all exported\n- [ ] All actions marked `'use server'`\n- [ ] `revalidatePath('/')` called after every mutation\n- [ ] Title sanitized: HTML stripped, length validated (1–500 chars)\n- [ ] `due_date` validated against ISO 8601 date regex before insert\n- [ ] TypeScript compiles without errors\n\n## Technical Notes\n- SECURITY: `sanitizeTitle` strips `<[^>]*>` — prevents HTML injection; React escapes on render anyway but defense-in-depth\n- SECURITY: Drizzle fully parameterizes all queries — no SQL injection risk\n- SECURITY: `due_date` regex `/^\\d{4}-\\d{2}-\\d{2}$/` rejects non-date strings\n- Do NOT use `dangerouslySetInnerHTML` anywhere in the UI — use React's default escaping\n- `revalidatePath('/')` invalidates Next.js cache — triggers RSC re-render with fresh data\n- `TURSO_AUTH_TOKEN` must never appear in this file — only via `lib/db.ts` from env","status":"closed","priority":0,"issue_type":"task","created_at":"2026-02-27T04:03:52.109053229Z","created_by":"quypq","updated_at":"2026-02-27T04:30:57.598483849Z","closed_at":"2026-02-27T04:30:57.598037050Z","close_reason":"Server Actions CRUD implemented in src/actions/todos.ts with 'use server' directive. Functions: createTodo(title, dueDate?), toggleTodo(id), updateTodo(id, title, dueDate?), deleteTodo(id). Input validation: title trimmed and max 500 chars. completed stored as 0/1 integer. revalidatePath('/') called after each mutation. TypeScript compiles without errors.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1xj","depends_on_id":"hdkit-project-1-16j","type":"blocks","created_at":"2026-02-27T04:03:52.109053229Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1y2","title":"Todo item spectrum left-border color assignment","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T09:22:24.693343135Z","created_by":"quypq","updated_at":"2026-02-27T09:35:10.800200627Z","closed_at":"2026-02-27T09:35:10.798126931Z","close_reason":"Added spectrum left-border color to todo items: border-l-4 with color cycling through 7 spectrum colors based on todo.id","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1y2","depends_on_id":"hdkit-project-1-1e5","type":"blocks","created_at":"2026-02-27T09:22:31.336143964Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1yn","title":"Scaffold Next.js 16 project","description":"Initialize the project using `create-next-app@16` with TypeScript, App Router, and ESLint.\n\n## Steps\n1. Run: `npx create-next-app@16 . --typescript --eslint --app --tailwind --src-dir --import-alias \"@/*\"`\n2. Verify `package.json` lists `next@^16.x`\n3. Confirm `.gitignore` includes `.env.local` and `local.db`\n4. Delete the default `app/page.tsx` placeholder content (keep structure)\n5. Confirm `turbopack` is the default dev server (Next.js 16.1 default — no `--turbopack` flag needed)\n\n## Acceptance Criteria\n- [ ] `npm run dev` starts successfully on localhost:3000\n- [ ] App Router directory structure exists under `src/app/`\n- [ ] TypeScript compiles without errors\n- [ ] `.gitignore` contains `.env.local` and `local.db`\n- [ ] `package.json` shows `next@^16`\n\n## Technical Notes\n- Node v24.12.0, npm 11.6.2 — confirmed compatible\n- Do NOT add a `tailwind.config.js` — Tailwind v4 uses CSS-first config (handled in bead 2)\n- The scaffold may generate a v3-style tailwind config; if so, delete it","status":"closed","priority":0,"issue_type":"task","created_at":"2026-02-27T04:02:41.122453858Z","created_by":"quypq","updated_at":"2026-02-27T04:28:59.898122372Z","closed_at":"2026-02-27T04:28:59.897858872Z","close_reason":"Scaffolded Next.js 16 manually: package.json with scripts (dev/build/start/lint/typecheck), tsconfig.json, next.config.ts with serverExternalPackages=['@libsql/client'], postcss.config.mjs, .gitignore with .env.local and local.db, App Router structure under src/app/ with layout.tsx, page.tsx, globals.css. next@16.1.6, react@19.2.4, tailwindcss@4 installed.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1yn","depends_on_id":"hdkit-project-1-282","type":"blocks","created_at":"2026-02-27T04:02:41.122453858Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-1yv","title":"Configure next.config.ts (serverExternalPackages)","description":"Configure next.config.ts to externalize @libsql/client so Turbopack/Webpack does not attempt to bundle it.\n\n## Steps\n1. Open (or create) `next.config.ts` in project root\n2. Add `serverExternalPackages: ['@libsql/client']` to the Next.js config object\n3. Confirm no custom `webpack()` function is added (would conflict with Turbopack)\n4. Restart `npm run dev` and confirm it compiles cleanly\n\n## Example\n```ts\nimport type { NextConfig } from 'next'\n\nconst nextConfig: NextConfig = {\n  serverExternalPackages: ['@libsql/client'],\n}\n\nexport default nextConfig\n```\n\n## Acceptance Criteria\n- [ ] `next.config.ts` contains `serverExternalPackages: ['@libsql/client']`\n- [ ] No `webpack()` override in config\n- [ ] `npm run dev` compiles without module parse errors\n\n## Technical Notes\n- CRITICAL: Without this setting, Turbopack/Webpack tries to bundle @libsql/client and fails\n  parsing its README.md as JavaScript — produces a cryptic \"unexpected token\" build error\n- This bead MUST be complete before bead 4 (Drizzle schema + DB client) is started, because\n  importing @libsql/client without the external config crashes `next dev`\n- Next.js 16.1 uses Turbopack by default — no need for `experimental.turbo`","status":"closed","priority":0,"issue_type":"task","created_at":"2026-02-27T04:03:12.320733222Z","created_by":"quypq","updated_at":"2026-02-27T04:30:00.314636031Z","closed_at":"2026-02-27T04:30:00.313869431Z","close_reason":"next.config.ts configured with serverExternalPackages: ['@libsql/client'] — CRITICAL setting preventing next dev crash. File created at /home/quypq/projects/hdkit-project-1/next.config.ts with proper TypeScript NextConfig type.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-1yv","depends_on_id":"hdkit-project-1-1yn","type":"blocks","created_at":"2026-02-27T04:03:12.320733222Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-263","title":"Create Zustand UI store","description":"Create a minimal Zustand store for client-side UI state (filter and editing state only).\n\n## Steps\n1. Install Zustand: `npm install zustand`\n2. Create `src/store/ui.ts`:\n\n```ts\nimport { create } from 'zustand'\n\ntype Filter = 'all' | 'active' | 'completed'\n\ninterface UIStore {\n  filter: Filter\n  editingId: number | null\n  setFilter: (filter: Filter) => void\n  setEditingId: (id: number | null) => void\n}\n\nexport const useUIStore = create<UIStore>((set) => ({\n  filter:      'all',\n  editingId:   null,\n  setFilter:   (filter)    => set({ filter }),\n  setEditingId: (editingId) => set({ editingId }),\n}))\n```\n\n## Acceptance Criteria\n- [ ] `src/store/ui.ts` exports `useUIStore`\n- [ ] `filter` defaults to `'all'`\n- [ ] `editingId` defaults to `null`\n- [ ] TypeScript infers types correctly (no `any`)\n- [ ] No todo data stored in client state (data lives in the DB, fetched by RSC)\n\n## Technical Notes\n- Intentionally minimal — no persistence (filter resets on refresh is acceptable)\n- No todo CRUD in this store — server state lives in DB, refreshed via revalidatePath\n- Store must be created BEFORE FilterTabs, TodoForm, and TodoItem components\n  because they all import from this file","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:04:03.409844086Z","created_by":"quypq","updated_at":"2026-02-27T04:34:32.197260997Z","closed_at":"2026-02-27T04:34:32.196751897Z","close_reason":"Created Zustand UI store with filter (all/active/completed) and editingId state in src/store/ui.ts","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-263","depends_on_id":"hdkit-project-1-1yn","type":"blocks","created_at":"2026-02-27T04:04:03.409844086Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-282","title":"Epic: Todo App — Next.js 16","description":"Build a modern, visually polished todo list web app using Next.js 16 App Router.\n\n## Stack\n- Next.js 16.1 (App Router, Turbopack, Server Actions)\n- Tailwind CSS v4 + shadcn/ui (new-york style)\n- Framer Motion (microinteractions)\n- Zustand (filter + UI state)\n- Drizzle ORM + @libsql/client → SQLite dev / Turso prod\n- next-themes (dark mode)\n- Vercel deployment\n\n## Features\n- Create / complete / delete / edit todos\n- Filter: All / Active / Completed with count badges\n- Due dates with color-coded display (green/amber/red)\n- Dark mode default (system preference)\n- Glassmorphism card UI\n- Keyboard shortcuts: n=new, Enter=save, Esc=cancel, Delete=remove focused\n\n## Acceptance Criteria\n- [ ] CRUD operations work end-to-end\n- [ ] Filter state persists\n- [ ] Due dates render color-coded correctly\n- [ ] Dark/light mode toggles without flash\n- [ ] Animations don't block interaction\n- [ ] Deploys to Vercel in one `git push`\n- [ ] Lighthouse ≥ 90 performance and accessibility","status":"closed","priority":0,"issue_type":"epic","created_at":"2026-02-27T04:02:16.224148437Z","created_by":"quypq","updated_at":"2026-02-27T04:42:26.710810823Z","closed_at":"2026-02-27T04:42:26.710580423Z","close_reason":"All 17 beads completed across 4 parallel tracks. Next.js 16 Todo App fully built: scaffold, Drizzle+Turso data layer, server actions, Tailwind v4+shadcn/ui, ThemeProvider+glassmorphism, Zustand store, FilterTabs, TodoForm, TodoItem, TodoList, Framer Motion microinteractions, git repo initialized.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"hdkit-project-1-28a","title":"Deploy to Vercel with Turso production DB","description":"Set up the Turso production database and deploy the app to Vercel.\n\n## Pre-requisites\n- Turso account (free tier sufficient): https://turso.tech\n- Vercel account: https://vercel.com\n- GitHub repo with code pushed\n\n## Steps\n\n### 1. Create Turso Database\n```bash\nturso db create todo-app\nturso db show todo-app          # Get the libsql:// URL\nturso db tokens create todo-app # Get auth token\n```\n\n### 2. Run Production Migration\n```bash\n# Set env vars temporarily for migration\nTURSO_DATABASE_URL=libsql://your-db.turso.io \\\nTURSO_AUTH_TOKEN=your-token \\\nnpm run db:migrate\n```\n\n### 3. Push to GitHub\n```bash\ngit add .\ngit commit -m \"feat: initial todo app\"\ngit push origin main\n```\n\n### 4. Deploy to Vercel\n```bash\nvercel --prod\n# OR via Vercel dashboard: import GitHub repo\n```\n\n### 5. Set Vercel Environment Variables\nIn Vercel project settings → Environment Variables:\n- `TURSO_DATABASE_URL` = `libsql://your-db.turso.io`\n- `TURSO_AUTH_TOKEN` = `your-token`\n\n### 6. Trigger Redeployment\n```bash\nvercel --prod  # or push a new commit\n```\n\n## Acceptance Criteria\n- [ ] Turso DB created and migration applied (todos table exists in prod)\n- [ ] Vercel project linked to GitHub repo\n- [ ] `TURSO_DATABASE_URL` and `TURSO_AUTH_TOKEN` set in Vercel env vars\n- [ ] Production URL is accessible and fully functional\n- [ ] CRUD operations work on the deployed app\n- [ ] Dark mode works on production\n- [ ] `.env.local` is NOT committed to git\n\n## Technical Notes\n- SECURITY: `TURSO_AUTH_TOKEN` must only exist in Vercel dashboard — never in git\n- `drizzle-kit migrate` (not `push`) for prod — generates and applies migration files\n- The `migrations/` directory SHOULD be committed to git (version-controlled schema)\n- `serverExternalPackages: ['@libsql/client']` in next.config.ts ensures Vercel\n  serverless functions can import @libsql/client correctly\n- Vercel free tier is sufficient for personal/portfolio use\n- If Vercel deployment fails with module errors, verify serverExternalPackages is set","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T04:08:13.763596659Z","created_by":"quypq","updated_at":"2026-02-27T04:42:06.080000048Z","closed_at":"2026-02-27T04:42:06.079735648Z","close_reason":"Git repo initialized, initial commit created (24 files). turso and vercel CLIs not available in this environment. TypeScript compiles clean. Manual deploy steps documented: 1) Create Turso DB via dashboard or CLI, 2) Set TURSO_DATABASE_URL and TURSO_AUTH_TOKEN, 3) Run drizzle-kit migrate, 4) Deploy to Vercel via dashboard or CLI with env vars set. Local dev: TURSO_DATABASE_URL=file:./local.db","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-28a","depends_on_id":"hdkit-project-1-39x","type":"blocks","created_at":"2026-02-27T04:08:13.763596659Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-28a","depends_on_id":"hdkit-project-1-3cp","type":"blocks","created_at":"2026-02-27T04:08:13.763596659Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-28a","depends_on_id":"hdkit-project-1-3ql","type":"blocks","created_at":"2026-02-27T04:08:13.763596659Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-28a","depends_on_id":"hdkit-project-1-8pt","type":"blocks","created_at":"2026-02-27T04:08:13.763596659Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-28a","depends_on_id":"hdkit-project-1-f6o","type":"blocks","created_at":"2026-02-27T04:08:13.763596659Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-2j3","title":"UI Improvement — Full Project","description":"## Overview\n\nComprehensive UI improvement pass for the hdkit-project-1 Todo App. Transform the current neutral glassmorphism design into a bold, cohesive violet/indigo identity with improved UX patterns across all components.\n\n## Scope\n\n- Add violet/indigo accent color system to globals.css\n- Retheme background gradient from slate to violet/indigo\n- Add app branding to header (CheckSquare icon + wordmark)\n- Collapse datetime picker behind a toggle in the form\n- Harmonize todo items with glassmorphism container (glass-surface)\n- Full-width filter tabs with accent active state\n- New TodoProgress bar component\n- Improved empty state with ClipboardList icon\n- Visual QA pass (build + lint + typecheck)\n\n## Acceptance Criteria\n\n- [ ] All 9 child tasks completed and passing\n- [ ] npm run build passes with no errors\n- [ ] npm run typecheck passes\n- [ ] npm run lint passes\n- [ ] Light mode: violet gradient, branded header, glass items, accent tabs visible\n- [ ] Dark mode: all above correct; glassmorphism depth visible through items\n- [ ] No hydration warnings in browser console","status":"closed","priority":1,"issue_type":"epic","created_at":"2026-02-27T08:29:33.250772083Z","created_by":"quypq","updated_at":"2026-02-27T09:01:53.593629424Z","closed_at":"2026-02-27T09:01:53.592798726Z","close_reason":"All 9 beads complete across 5 parallel tracks. UI now features violet/indigo theme, branded glass-header, collapsible datetime picker, glass-surface TodoItems, full-width violet FilterTabs, TodoProgress bar, and styled empty state. TypeScript clean, production build passes.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"hdkit-project-1-2lj","title":"Add violet/indigo accent color tokens to globals.css","description":"## Context\n\nThis is the foundational bead for the full UI improvement. All subsequent beads reference `text-accent-violet` and `bg-accent-violet` Tailwind utility classes, which are generated from the tokens defined here. Complete this bead first.\n\n## What to Do\n\nEdit `src/app/globals.css` to add violet/indigo accent tokens:\n\n**In `:root` block, REPLACE these existing neutral tokens:**\n```css\n--primary:              oklch(0.6 0.22 270);\n--primary-foreground:   oklch(0.98 0.01 270);\n--accent:               oklch(0.94 0.04 270);\n--accent-foreground:    oklch(0.35 0.18 270);\n--ring:                 oklch(0.6 0.22 270);\n```\n\n**In `:root` block, ADD these new violet alias tokens:**\n```css\n--accent-violet:        oklch(0.6 0.22 270);\n--accent-violet-dark:   oklch(0.55 0.25 270);\n--accent-violet-muted:  oklch(0.94 0.04 270);\n```\n\n**In `.dark` block, REPLACE:**\n```css\n--primary:              oklch(0.65 0.22 270);\n--primary-foreground:   oklch(0.98 0.01 270);\n--accent:               oklch(0.269 0.06 270);\n--accent-foreground:    oklch(0.88 0.06 270);\n--ring:                 oklch(0.55 0.22 270);\n```\n\n**In `.dark` block, ADD:**\n```css\n--accent-violet:        oklch(0.65 0.22 270);\n--accent-violet-dark:   oklch(0.55 0.25 270);\n--accent-violet-muted:  oklch(0.30 0.08 270);\n```\n\n**In `@theme inline {}` block, ADD:**\n```css\n--color-accent-violet:       var(--accent-violet);\n--color-accent-violet-dark:  var(--accent-violet-dark);\n--color-accent-violet-muted: var(--accent-violet-muted);\n```\n\n## Side Effects (free, no changes needed)\n\n- `Checkbox` checked state (`data-[state=checked]:bg-primary`) → automatically violet\n- `Button` default variant (`bg-primary`) → automatically violet Add button\n- Focus rings (`--ring`) → violet\n\n## Technical Notes\n\n- Tailwind v4 CSS-first: NO tailwind.config.js. All tokens live in globals.css under `@theme inline {}`\n- All glassmorphism tokens (`--glass-bg`, `--glass-border`, etc.) are UNCHANGED\n- Run `npm run build` to verify Tailwind parses the new tokens correctly\n\n## Acceptance Criteria\n\n- [ ] `--primary`, `--accent`, `--ring` in `:root` have hue 270 and non-zero chroma\n- [ ] Same tokens updated in `.dark` block\n- [ ] `--color-accent-violet`, `--color-accent-violet-dark`, `--color-accent-violet-muted` added to `@theme inline`\n- [ ] Existing glassmorphism tokens unchanged\n- [ ] `npm run build` passes with no Tailwind parse errors\n- [ ] `npm run typecheck` passes (CSS-only change)","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T08:30:38.934421043Z","created_by":"quypq","updated_at":"2026-02-27T08:54:34.316531999Z","closed_at":"2026-02-27T08:54:34.316224097Z","close_reason":"Added violet/indigo accent color tokens to globals.css: updated --primary, --accent, --ring in :root and .dark to hue 270, added --accent-violet, --accent-violet-dark, --accent-violet-muted tokens in both :root and .dark, added --color-accent-violet*, --color-accent-violet-dark*, --color-accent-violet-muted* to @theme inline block. Build and typecheck pass.","source_repo":".","compaction_level":0,"original_size":0}
{"id":"hdkit-project-1-2rb","title":"Build TodoItem component (functional, no animations)","description":"Build the functional todo item component with toggle, edit, delete, due date display, and keyboard Delete shortcut.\n\n## Steps\n1. Create `src/components/todo-item.tsx`:\n\n```tsx\n'use client'\nimport { useTransition, useState, useRef } from 'react'\nimport { Checkbox } from '@/components/ui/checkbox'\nimport { Button } from '@/components/ui/button'\nimport { Input } from '@/components/ui/input'\nimport { Trash2, Pencil, Check, X } from 'lucide-react'\nimport { toggleTodo, deleteTodo, updateTodo } from '@/actions/todos'\nimport type { Todo } from '@/lib/schema'\n\n// Due date color utility\nfunction dueDateColor(dueDate: string | null): string {\n  if (!dueDate) return ''\n  const today = new Date().toISOString().slice(0, 10)\n  if (dueDate < today) return 'text-red-400'\n  if (dueDate === today) return 'text-amber-400'\n  return 'text-green-400'\n}\n\ninterface Props { todo: Todo }\n\nexport function TodoItem({ todo }: Props) {\n  const [isPending, startTransition] = useTransition()\n  const [isEditing, setIsEditing] = useState(false)\n  const editRef = useRef<HTMLInputElement>(null)\n\n  function handleDelete(e: React.KeyboardEvent | React.MouseEvent) {\n    startTransition(async () => { await deleteTodo(todo.id) })\n  }\n\n  function handleToggle() {\n    startTransition(async () => { await toggleTodo(todo.id, !todo.completed) })\n  }\n\n  function handleEditSave() {\n    const newTitle = editRef.current?.value.trim()\n    if (!newTitle) return\n    startTransition(async () => {\n      await updateTodo(todo.id, newTitle, todo.due_date ?? undefined)\n      setIsEditing(false)\n    })\n  }\n\n  return (\n    <li\n      className=\"flex items-center gap-3 p-3 rounded-xl bg-white/5 border border-white/10 hover:bg-white/10 transition-colors group\"\n      onKeyDown={e => {\n        if (e.key === 'Delete' && e.target === e.currentTarget) handleDelete(e)\n      }}\n      tabIndex={0}\n    >\n      <Checkbox\n        checked={!!todo.completed}\n        onCheckedChange={handleToggle}\n        disabled={isPending}\n        className=\"border-white/30\"\n      />\n      {isEditing ? (\n        <Input\n          ref={editRef}\n          defaultValue={todo.title}\n          className=\"flex-1 h-7 text-sm bg-transparent border-white/20 text-white\"\n          onKeyDown={e => {\n            if (e.key === 'Enter') handleEditSave()\n            if (e.key === 'Escape') setIsEditing(false)\n          }}\n          autoFocus\n        />\n      ) : (\n        <span className={`flex-1 text-sm text-white ${todo.completed ? 'line-through opacity-50' : ''}`}>\n          {todo.title}\n        </span>\n      )}\n      {todo.due_date && (\n        <span className={`text-xs ${dueDateColor(todo.due_date)}`}>{todo.due_date}</span>\n      )}\n      {isEditing ? (\n        <>\n          <Button variant=\"ghost\" size=\"icon\" className=\"h-7 w-7 text-white/60 hover:text-white\" onClick={handleEditSave}><Check className=\"h-3 w-3\" /></Button>\n          <Button variant=\"ghost\" size=\"icon\" className=\"h-7 w-7 text-white/60 hover:text-white\" onClick={() => setIsEditing(false)}><X className=\"h-3 w-3\" /></Button>\n        </>\n      ) : (\n        <>\n          <Button variant=\"ghost\" size=\"icon\" className=\"h-7 w-7 text-white/60 hover:text-white opacity-0 group-hover:opacity-100\" onClick={() => setIsEditing(true)}><Pencil className=\"h-3 w-3\" /></Button>\n          <Button variant=\"ghost\" size=\"icon\" className=\"h-7 w-7 text-white/60 hover:text-red-400 opacity-0 group-hover:opacity-100\" onClick={handleDelete}><Trash2 className=\"h-3 w-3\" /></Button>\n        </>\n      )}\n    </li>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] Checkbox toggles `completed` state via `toggleTodo` server action\n- [ ] Completed todos show `line-through` styling\n- [ ] Edit mode: click pencil icon → inline input → Enter saves, Esc cancels\n- [ ] Delete button calls `deleteTodo`\n- [ ] `Delete` keyboard key on focused `<li>` deletes the item\n- [ ] `due_date` rendered with correct color (green/amber/red or none)\n- [ ] Edit/delete buttons only visible on hover (`group-hover:opacity-100`)\n- [ ] `useTransition` prevents UI blocking during server actions\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism item card (bg-white/5, border-white/10), color-coded due dates (green=future, amber=today, red=overdue), dark mode. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- `completed` is `0/1` integer — use `!!todo.completed` to convert to boolean\n- `dueDateColor` compares ISO strings directly (lexicographic = chronological for YYYY-MM-DD)\n- Animations (Framer Motion) are added in bead 15 — this bead implements functionality only\n- `tabIndex={0}` on `<li>` enables keyboard focus for Delete shortcut\n- Guard Delete key: `e.target === e.currentTarget` prevents firing when deleting text in edit input","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:07:17.427970955Z","created_by":"quypq","updated_at":"2026-02-27T04:38:32.733605167Z","closed_at":"2026-02-27T04:38:32.731265467Z","close_reason":"Created TodoItem client component with checkbox toggle, inline title editing (Enter=save, Esc=cancel), due date color logic (red=overdue, yellow=today, normal=future), Delete key to delete, calls toggleTodo/updateTodo/deleteTodo server actions","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-2rb","depends_on_id":"hdkit-project-1-1xj","type":"blocks","created_at":"2026-02-27T04:07:17.427970955Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-2rb","depends_on_id":"hdkit-project-1-263","type":"blocks","created_at":"2026-02-27T04:07:17.427970955Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-2rb","depends_on_id":"hdkit-project-1-2xr","type":"blocks","created_at":"2026-02-27T04:07:17.427970955Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-2vh","title":"Build root layout with ThemeProvider","description":"Set up the Next.js root layout with next-themes for dark mode support.\n\n## Steps\n1. Install: `npm install next-themes`\n2. Create/update `src/app/layout.tsx`:\n\n```tsx\nimport type { Metadata } from 'next'\nimport { ThemeProvider } from 'next-themes'\nimport './globals.css'\n\nexport const metadata: Metadata = {\n  title: 'Todo',\n  description: 'Modern todo app',\n}\n\nexport default function RootLayout({ children }: { children: React.ReactNode }) {\n  return (\n    <html lang=\"en\" suppressHydrationWarning>\n      <body>\n        <ThemeProvider attribute=\"class\" defaultTheme=\"system\" enableSystem>\n          {children}\n        </ThemeProvider>\n      </body>\n    </html>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] `ThemeProvider` wraps all children\n- [ ] `attribute=\"class\"` — Tailwind `dark:` classes work\n- [ ] `defaultTheme=\"system\"` — respects OS dark mode preference\n- [ ] `suppressHydrationWarning` on `<html>` — prevents hydration mismatch\n- [ ] `npm run dev` renders without React hydration warnings\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism card (backdrop-blur, semi-transparent bg), dark mode default via next-themes + Tailwind dark: classes. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- `suppressHydrationWarning` is required on `<html>` — next-themes dynamically\n  adds the `dark` class, which causes a mismatch between server and client render\n- `ThemeProvider` must exist BEFORE any component uses `useTheme()` —\n  if ThemeToggle renders outside this provider, it throws\n- Do NOT add `font-*` classes or Google Fonts imports in this bead unless\n  explicitly in scope — keep layout minimal","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:04:15.179577025Z","created_by":"quypq","updated_at":"2026-02-27T04:37:12.160174257Z","closed_at":"2026-02-27T04:37:12.159269257Z","close_reason":"Updated src/app/layout.tsx with ThemeProvider from next-themes. suppressHydrationWarning on html, attribute='class', defaultTheme='system', enableSystem.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-2vh","depends_on_id":"hdkit-project-1-2xr","type":"blocks","created_at":"2026-02-27T04:04:15.179577025Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-2xr","title":"Configure Tailwind CSS v4 + shadcn/ui","description":"Set up Tailwind CSS v4 (CSS-first config) and initialize shadcn/ui with the new-york style.\n\n## Steps\n1. Delete any `tailwind.config.js` or `tailwind.config.ts` generated by scaffold\n2. Update `src/app/globals.css`:\n   - Replace `@tailwind base/components/utilities` with `@import \"tailwindcss\"`\n   - Add `@import \"tw-animate-css\"` (replaces tailwindcss-animate)\n   - Add `@theme` block with OKLCH color palette for shadcn\n   - Add glassmorphism custom tokens (handled in bead 16; just establish structure here)\n3. Install: `npm install tw-animate-css`\n4. Run: `npx shadcn@latest init` — select:\n   - Style: `new-york`\n   - Color: `zinc` (or neutral — matches glassmorphism palette)\n   - Tailwind CSS: Yes (v4 path)\n5. Add required components: `npx shadcn@latest add button input checkbox tabs`\n6. Verify `src/components/ui/` directory was created\n\n## Acceptance Criteria\n- [ ] No `tailwind.config.js` in project root\n- [ ] `globals.css` uses `@import \"tailwindcss\"` (v4 syntax)\n- [ ] `src/components/ui/button.tsx`, `input.tsx`, `checkbox.tsx`, `tabs.tsx` exist\n- [ ] `npm run dev` still compiles without errors after changes\n- [ ] Dark mode classes (`dark:bg-*`) render correctly on manual toggle\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism card (backdrop-blur, semi-transparent bg), Framer Motion microinteractions (checkbox scale bounce, strike-through on complete, slide-out on delete via AnimatePresence), color-coded due dates (green=future, amber=today, red=overdue), shadcn/ui Tabs with count badges for filters, dark mode default via next-themes + Tailwind dark: classes. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- Tailwind v4 BREAKING: no `tailwind.config.js`; all config via `@theme` in CSS\n- Use `tw-animate-css` NOT `tailwindcss-animate` (v4 incompatible)\n- shadcn's CLI detects Tailwind v4 automatically on recent versions — use `npx shadcn@latest`\n- OKLCH colors: shadcn v4 uses OKLCH instead of HSL — do not manually convert\n- `forwardRef` removed from shadcn components in 2025 — no action needed on fresh init","status":"closed","priority":0,"issue_type":"task","created_at":"2026-02-27T04:03:02.101584541Z","created_by":"quypq","updated_at":"2026-02-27T04:35:53.935022494Z","closed_at":"2026-02-27T04:35:53.934810794Z","close_reason":"Configured Tailwind CSS v4 + shadcn/ui new-york style with zinc base color. CSS variables, OKLCH palette, tw-animate-css. Installed: tabs, input, checkbox, button, dialog components. No tailwind.config.js (CSS-first). utils.ts created.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-2xr","depends_on_id":"hdkit-project-1-1yn","type":"blocks","created_at":"2026-02-27T04:03:02.101584541Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-2yu","title":"Collapse datetime picker behind CalendarDays toggle in todo-form.tsx","description":"## Context\n\nThe current TodoForm has a single row with title input, a fixed 192px datetime-local input, and the Add button — the datetime picker is always visible even when the user doesn't want a due date. This bead collapses it behind a toggle button.\n\n## What to Do\n\nEdit `src/components/todo-form.tsx`:\n\n**1. Add `useState` (already imported — just add to the import):**\nAlready: `import { useRef, useEffect, useCallback } from 'react'`\nChange to: `import { useRef, useEffect, useCallback, useState } from 'react'`\n\n**2. Add `CalendarDays` to lucide-react import:**\n```tsx\nimport { CalendarDays } from 'lucide-react'\n```\n\n**3. Add state at top of component:**\n```tsx\nconst [showDueDate, setShowDueDate] = useState(false)\n```\n\n**4. Restructure the JSX return:**\n```tsx\nreturn (\n  <form onSubmit={handleSubmit} className=\"flex flex-col gap-2\">\n    {/* Row 1: title + toggle + submit */}\n    <div className=\"flex gap-2 items-center\">\n      <Input\n        ref={titleRef}\n        type=\"text\"\n        placeholder=\"Add a new todo… (press 'n' to focus)\"\n        onKeyDown={handleKeyDown}\n        className=\"flex-1\"\n        autoComplete=\"off\"\n      />\n      <Button\n        type=\"button\"\n        variant=\"ghost\"\n        size=\"icon-sm\"\n        onClick={() => setShowDueDate((v) => !v)}\n        aria-label={showDueDate ? 'Hide due date' : 'Add due date'}\n        aria-expanded={showDueDate}\n        className={cn(\n          'text-muted-foreground shrink-0',\n          showDueDate && 'text-accent-violet'\n        )}\n      >\n        <CalendarDays size={16} aria-hidden=\"true\" />\n      </Button>\n      <Button type=\"submit\">Add</Button>\n    </div>\n\n    {/* Row 2: due date — collapsible */}\n    {showDueDate && (\n      <Input\n        ref={dueDateRef}\n        type=\"datetime-local\"\n        onKeyDown={handleKeyDown}\n        className=\"w-full\"\n        aria-label=\"Due date and time\"\n      />\n    )}\n  </form>\n)\n```\n\nNote: `cn` is already imported from `@/lib/utils`. Add it to the import if not present.\n\n## Behavior Notes\n\n- When `showDueDate` is false, `dueDateRef.current` is null. `dueDateRef.current?.value` returns `undefined` → the optional chaining handles this safely. The form submission works correctly with no due date.\n- `handleClear` already uses optional chaining: `if (dueDateRef.current) dueDateRef.current.value = ''` — safe when null.\n- Pressing Esc still clears the title input and blurs. Optionally `setShowDueDate(false)` in `handleClear` to also hide the picker.\n\n## Technical Notes\n\n- `useState` local to this component — NOT in Zustand store (ephemeral UI state)\n- `cn` import: `import { cn } from '@/lib/utils'` — add if not present\n- `size=\"icon-sm\"` may require checking `src/components/ui/button.tsx` for the `icon-sm` size variant. If it doesn't exist, use `size=\"sm\"` and adjust padding.\n\n## UI Standard\n\n- Pattern: form\n- Standard: inline — two-row layout; datetime collapsed by default; CalendarDays toggle turns accent-violet when open\n\n## Acceptance Criteria\n\n- [ ] Default state: single row (title input + CalendarDays toggle + Add button)\n- [ ] Clicking toggle: datetime-local input appears in row 2 below\n- [ ] Clicking toggle again: datetime-local input collapses\n- [ ] CalendarDays button is `text-accent-violet` when expanded\n- [ ] Submitting without opening due date picker: no due date sent (no console errors)\n- [ ] Keyboard shortcuts (n to focus, Enter to submit, Esc to clear) all work\n- [ ] `npm run typecheck` passes","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T08:35:25.237848076Z","created_by":"quypq","updated_at":"2026-02-27T08:56:09.632061303Z","closed_at":"2026-02-27T08:56:09.628938118Z","close_reason":"Done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-2yu","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:35.532777941Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-36t","title":"Build main page (RSC)","description":"Build the main RSC page that fetches todos from the database and composes the UI.\n\n## Steps\n1. Create/update `src/app/page.tsx`:\n\n```tsx\nimport { db } from '@/lib/db'\nimport { todos } from '@/lib/schema'\nimport { TodoForm } from '@/components/todo-form'\nimport { TodoList } from '@/components/todo-list'\nimport { FilterTabs } from '@/components/filter-tabs'\nimport { ThemeToggle } from '@/components/theme-toggle'\n\nexport default async function Page() {\n  const allTodos = await db.select().from(todos).orderBy(todos.created_at)\n\n  return (\n    <main className=\"min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 dark:from-slate-950 dark:to-slate-900 flex items-center justify-center p-4\">\n      <div className=\"w-full max-w-lg\">\n        <div className=\"flex items-center justify-between mb-6\">\n          <h1 className=\"text-2xl font-bold text-white\">Todos</h1>\n          <ThemeToggle />\n        </div>\n        <div className=\"backdrop-blur-xl bg-white/10 dark:bg-white/5 border border-white/20 rounded-2xl p-6 shadow-2xl\">\n          <TodoForm />\n          <FilterTabs todos={allTodos} />\n          <TodoList todos={allTodos} />\n        </div>\n      </div>\n    </main>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] Page is an `async` RSC (no `'use client'`)\n- [ ] DB query runs server-side: `db.select().from(todos)`\n- [ ] `TodoForm`, `TodoList`, `FilterTabs`, `ThemeToggle` all render\n- [ ] Glassmorphism container: `backdrop-blur-xl bg-white/10 border border-white/20 rounded-2xl`\n- [ ] Gradient background spans full screen\n- [ ] Page renders todos (even if list is empty) without runtime errors\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism card (backdrop-blur, semi-transparent bg), Framer Motion microinteractions, color-coded due dates, shadcn/ui Tabs with count badges for filters, dark mode default via next-themes + Tailwind dark: classes. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- RSC can directly import `db` from `@/lib/db` — no API route needed\n- `allTodos` is passed to both `TodoList` (to render) and `FilterTabs` (for counts)\n- Filtering is client-side (Zustand) — pass ALL todos to both components,\n  each filters based on Zustand `filter` state\n- Do NOT wrap the page in `'use client'` — this breaks RSC data fetching","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:04:28.595475102Z","created_by":"quypq","updated_at":"2026-02-27T04:39:07.509799111Z","closed_at":"2026-02-27T04:39:07.506227911Z","close_reason":"Built src/app/page.tsx as async RSC with DB query (desc order by created_at), glassmorphism container, renders TodoForm, FilterTabs, TodoList. No 'use client'. Gradient background spans full screen.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-36t","depends_on_id":"hdkit-project-1-1xj","type":"blocks","created_at":"2026-02-27T04:04:28.595475102Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-36t","depends_on_id":"hdkit-project-1-263","type":"blocks","created_at":"2026-02-27T04:04:28.595475102Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-36t","depends_on_id":"hdkit-project-1-2vh","type":"blocks","created_at":"2026-02-27T04:04:28.595475102Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-38k","title":"Rainbow gradient progress bar","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T09:22:25.348809985Z","created_by":"quypq","updated_at":"2026-02-27T09:36:31.854382897Z","closed_at":"2026-02-27T09:36:31.851627907Z","close_reason":"Replaced solid progress bar color with 7-color spectrum rainbow gradient from coral/red to violet","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-38k","depends_on_id":"hdkit-project-1-1e5","type":"blocks","created_at":"2026-02-27T09:22:32.224768872Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-39x","title":"Build TodoList component (RSC)","description":"Build the RSC list container that renders filtered todos or an empty state message.\n\n## Steps\n1. Create `src/components/todo-list.tsx`:\n\n```tsx\n'use client'\nimport { useUIStore } from '@/store/ui'\nimport { TodoItem } from '@/components/todo-item'\nimport { AnimatePresence } from 'framer-motion'\nimport type { Todo } from '@/lib/schema'\n\ninterface Props { todos: Todo[] }\n\nexport function TodoList({ todos }: Props) {\n  const { filter } = useUIStore()\n\n  const filtered = todos.filter(todo => {\n    if (filter === 'active')    return !todo.completed\n    if (filter === 'completed') return  todo.completed\n    return true\n  })\n\n  if (filtered.length === 0) {\n    return (\n      <p className=\"text-center text-white/40 py-8 text-sm\">\n        {filter === 'completed' ? 'No completed tasks yet.' :\n         filter === 'active'    ? 'All caught up!' :\n         'No todos yet — add one above!'}\n      </p>\n    )\n  }\n\n  return (\n    <ul className=\"space-y-2\">\n      <AnimatePresence initial={false}>\n        {filtered.map(todo => (\n          <TodoItem key={todo.id} todo={todo} />\n        ))}\n      </AnimatePresence>\n    </ul>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] Renders only todos matching current Zustand `filter`\n- [ ] Shows context-aware empty state message when no matching todos\n- [ ] `AnimatePresence` wraps list for exit animations (bead 15 adds the motion)\n- [ ] Reads filter from Zustand (not props) — stays reactive to tab changes\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism card, Framer Motion microinteractions, color-coded due dates, dark mode. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- `'use client'` required to read Zustand filter state reactively\n- `AnimatePresence` should be installed even if Framer Motion animations are added\n  in bead 15 — this prepares the structure so bead 15 only needs to add motion props\n- `initial={false}` on AnimatePresence prevents initial mount animations\n- `completed` is integer 0/1 — use `!todo.completed` (truthy 0=false, 1=true works in JS)","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:04:53.185561080Z","created_by":"quypq","updated_at":"2026-02-27T04:39:30.789359186Z","closed_at":"2026-02-27T04:39:30.786294786Z","close_reason":"Built src/components/todo-list.tsx as client component. Uses useUIStore for filter state, AnimatePresence from framer-motion, renders TodoItem per filtered todo, context-aware empty state messages. TypeScript compiles clean.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-39x","depends_on_id":"hdkit-project-1-36t","type":"blocks","created_at":"2026-02-27T04:04:53.185561080Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-3cb","title":"Stretch FilterTabs to full width with violet accent on active tab","description":"## Context\n\nThe current FilterTabs `TabsList` floats left (default `w-fit inline-flex`) and uses a neutral gray active state. This bead stretches it to full container width and applies the violet accent to the active trigger.\n\n## What to Do\n\nEdit `src/components/filter-tabs.tsx`:\n\n**1. Add `className=\"w-full\"` to `<TabsList>`:**\n```tsx\n// BEFORE\n<TabsList>\n\n// AFTER\n<TabsList className=\"w-full\">\n```\n\n**2. Add accent className to ALL THREE `<TabsTrigger>` elements:**\n```tsx\n<TabsTrigger\n  value=\"all\"\n  className=\"data-[state=active]:bg-accent-violet data-[state=active]:text-white\"\n>\n  All\n  <span className=\"ml-1.5 rounded-full bg-muted-foreground/20 px-1.5 py-0.5 text-xs font-medium tabular-nums\">\n    {allCount}\n  </span>\n</TabsTrigger>\n\n<TabsTrigger\n  value=\"active\"\n  className=\"data-[state=active]:bg-accent-violet data-[state=active]:text-white\"\n>\n  Active\n  ...\n</TabsTrigger>\n\n<TabsTrigger\n  value=\"completed\"\n  className=\"data-[state=active]:bg-accent-violet data-[state=active]:text-white\"\n>\n  Completed\n  ...\n</TabsTrigger>\n```\n\n## Technical Notes\n\n- Tailwind v4 supports `data-[state=active]:` as an arbitrary data attribute variant natively\n- The `bg-accent-violet` class resolves to `var(--color-accent-violet)` which must be registered in `@theme inline` (done in Bead 1)\n- The shadcn TabsTrigger has `flex-1` in its base styles in `src/components/ui/tabs.tsx` — with `w-full` on TabsList, the three triggers automatically distribute evenly\n- Count badge spans inside triggers: the `bg-muted-foreground/20` is unchanged; when active it will be visible against the violet background\n\n## UI Standard\n\n- Pattern: filter tabs\n- Standard: inline — w-full TabsList, data-[state=active]:bg-accent-violet data-[state=active]:text-white on each TabsTrigger\n\n## Acceptance Criteria\n\n- [ ] `TabsList` has `className=\"w-full\"` — triggers distribute evenly across container\n- [ ] Active trigger shows violet background (`bg-accent-violet`) and white text\n- [ ] Count badges visible on all three tabs in active and inactive states\n- [ ] Filter behavior (click switches filter, TodoList re-renders) unaffected\n- [ ] `npm run typecheck` passes\n- [ ] No regressions in dark mode","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T08:39:12.959688644Z","created_by":"quypq","updated_at":"2026-02-27T08:56:29.029783Z","closed_at":"2026-02-27T08:56:29.029783Z","close_reason":"Added w-full to TabsList for full-width stretch. Applied data-[state=active]:bg-accent-violet data-[state=active]:text-white className to all three TabsTrigger elements (All, Active, Completed). Count badges preserved.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-3cb","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:36.442943307Z","created_by":"quypq","metadata":"{}","thread_id":""}]}
{"id":"hdkit-project-1-3cp","title":"Polish glassmorphism styles in globals.css","description":"Finalize the glassmorphism visual design by adding custom tokens to globals.css and applying them to the layout.\n\n## Steps\n1. Update `src/app/globals.css` to add custom glassmorphism tokens in `@theme`:\n\n```css\n@import \"tailwindcss\";\n@import \"tw-animate-css\";\n\n@theme {\n  /* Glassmorphism surface tokens */\n  --color-glass-bg: oklch(100% 0 0 / 0.08);\n  --color-glass-bg-dark: oklch(100% 0 0 / 0.04);\n  --color-glass-border: oklch(100% 0 0 / 0.18);\n  --color-glass-hover: oklch(100% 0 0 / 0.12);\n\n  /* Due date status colors */\n  --color-due-overdue: oklch(65% 0.22 29);   /* red-400 */\n  --color-due-today:   oklch(80% 0.17 84);   /* amber-400 */\n  --color-due-future:  oklch(72% 0.18 151);  /* green-400 */\n\n  /* Blur intensity */\n  --blur-glass: 20px;\n}\n```\n\n2. Verify backdrop-blur and bg-white/* utilities still work (should via Tailwind v4 defaults)\n3. Do a final visual pass in both dark and light mode:\n   - Card container: `backdrop-blur-xl bg-white/10 border-white/20`\n   - Item cards: `bg-white/5 border-white/10`\n   - Gradient bg: `from-slate-900 to-slate-800`\n4. Ensure dark mode (`.dark` class on html) swaps surface opacities correctly\n\n## Acceptance Criteria\n- [ ] `@theme` block defines glassmorphism tokens in `globals.css`\n- [ ] `backdrop-blur-xl` renders the frosted-glass effect on the card\n- [ ] Dark mode shows slightly more transparent surfaces than light mode\n- [ ] No `tailwind.config.js` present (v4 CSS-first)\n- [ ] Visual appearance matches brainstorm.md spec:\n  - Frosted-glass card for todo list container\n  - Gradient background (dark slate)\n  - Subtle item cards with hover highlight\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism card (backdrop-blur, semi-transparent bg, border-white/20). Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- OKLCH colors are the native color space for Tailwind v4 — do not use hex or hsl\n- `backdrop-blur` requires a semi-transparent background to be visible —\n  `bg-white/10` (10% opacity) is the sweet spot for dark glassmorphism\n- Test in both Chrome and Firefox — webkit-backdrop-filter may be needed:\n  add `[backdrop-filter:blur(20px)] [-webkit-backdrop-filter:blur(20px)]` if blur fails\n- Custom tokens in `@theme` are automatically available as Tailwind utilities:\n  `bg-glass-bg`, `border-glass-border`, etc.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T04:07:57.680101856Z","created_by":"quypq","updated_at":"2026-02-27T04:39:17.894488649Z","closed_at":"2026-02-27T04:39:17.892053549Z","close_reason":"Updated src/app/globals.css with glassmorphism utility classes: .glass, .glass-strong, .glass-card, .glass-surface, .glass-header, .glass-glow. Added CSS custom properties for glass-bg, glass-border, glass-blur, glass-shadow in both light and dark modes. Uses OKLCH color palette for consistent theming.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-3cp","depends_on_id":"hdkit-project-1-2vh","type":"blocks","created_at":"2026-02-27T04:07:57.680101856Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-3cp","depends_on_id":"hdkit-project-1-2xr","type":"blocks","created_at":"2026-02-27T04:07:57.680101856Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-3hf","title":"Implement Drizzle schema + DB client","description":"Set up Drizzle ORM with the libSQL/Turso adapter and define the todos table schema.\n\n## Steps\n1. Install dependencies:\n   ```bash\n   npm install drizzle-orm @libsql/client\n   npm install -D drizzle-kit\n   ```\n2. Create `src/lib/schema.ts`:\n   ```ts\n   import { integer, sqliteTable, text } from 'drizzle-orm/sqlite-core'\n\n   export const todos = sqliteTable('todos', {\n     id:         integer('id').primaryKey({ autoIncrement: true }),\n     title:      text('title').notNull(),\n     completed:  integer('completed').notNull().default(0),  // 0 = false, 1 = true\n     due_date:   text('due_date'),                           // ISO 8601 YYYY-MM-DD or null\n     created_at: text('created_at').notNull().default(sql`(datetime('now'))`),\n     updated_at: text('updated_at').notNull().default(sql`(datetime('now'))`),\n   })\n\n   export type Todo = typeof todos.$inferSelect\n   export type NewTodo = typeof todos.$inferInsert\n   ```\n3. Create `src/lib/db.ts`:\n   ```ts\n   import { drizzle } from 'drizzle-orm/libsql'\n\n   const url = process.env.TURSO_DATABASE_URL!\n   const authToken = process.env.TURSO_AUTH_TOKEN  // undefined OK for local file\n\n   export const db = drizzle({ connection: { url, authToken } })\n   ```\n4. Add `.env.local` (if not already present): `TURSO_DATABASE_URL=file:./local.db`\n\n## Acceptance Criteria\n- [ ] `src/lib/schema.ts` exports `todos` table and `Todo` / `NewTodo` types\n- [ ] `src/lib/db.ts` exports `db` instance using env-variable URL\n- [ ] TypeScript compiles without errors on both files\n- [ ] `completed` field typed as `integer` (0/1 boolean)\n\n## Technical Notes\n- `authToken` is optional for `file:./local.db` — do not throw if undefined\n- `sql` import needed for `default(sql`(datetime('now'))`)`:\n  `import { sql } from 'drizzle-orm'`\n- This bead depends on bead 3 (next.config.ts) being complete — importing\n  @libsql/client without serverExternalPackages crashes the dev server\n- `TURSO_AUTH_TOKEN` must NOT be committed to git — Vercel env vars only for prod","status":"closed","priority":0,"issue_type":"task","created_at":"2026-02-27T04:03:24.559384590Z","created_by":"quypq","updated_at":"2026-02-27T04:30:18.746303788Z","closed_at":"2026-02-27T04:30:18.745537188Z","close_reason":"Drizzle schema and DB client implemented. src/lib/schema.ts: todos table with id (integer PK autoincrement), title (text notNull), completed (integer default 0), due_date (text nullable), created_at (text), updated_at (text). src/lib/db.ts: drizzle-orm/libsql client using TURSO_DATABASE_URL env || 'file:./local.db'. drizzle-orm@0.43.1 and @libsql/client installed.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-3hf","depends_on_id":"hdkit-project-1-1yv","type":"blocks","created_at":"2026-02-27T04:03:24.559384590Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-3hg","title":"Colorful background gradient in page.tsx","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T09:22:24.368823680Z","created_by":"quypq","updated_at":"2026-02-27T09:34:54.373359712Z","closed_at":"2026-02-27T09:34:54.371002915Z","close_reason":"Added rainbow pastel gradient to page.tsx main element: rose→violet→cyan light, dark oklch spectrum gradient","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-3hg","depends_on_id":"hdkit-project-1-1e5","type":"blocks","created_at":"2026-02-27T09:22:30.918783664Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-3ql","title":"Build ThemeToggle component","description":"Build the dark/light mode toggle button using next-themes and lucide-react icons.\n\n## Steps\n1. Install lucide-react if not already present: `npm install lucide-react`\n2. Create `src/components/theme-toggle.tsx`:\n\n```tsx\n'use client'\nimport { useTheme } from 'next-themes'\nimport { useEffect, useState } from 'react'\nimport { Moon, Sun } from 'lucide-react'\nimport { Button } from '@/components/ui/button'\n\nexport function ThemeToggle() {\n  const { theme, setTheme } = useTheme()\n  const [mounted, setMounted] = useState(false)\n\n  useEffect(() => setMounted(true), [])\n\n  if (!mounted) return <div className=\"w-9 h-9\" />  // Prevent hydration mismatch\n\n  return (\n    <Button\n      variant=\"ghost\"\n      size=\"icon\"\n      onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}\n      className=\"text-white hover:bg-white/10\"\n      aria-label=\"Toggle theme\"\n    >\n      {theme === 'dark' ? <Sun className=\"h-4 w-4\" /> : <Moon className=\"h-4 w-4\" />}\n    </Button>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] Renders Sun icon in dark mode, Moon icon in light mode\n- [ ] Click toggles between dark and light\n- [ ] `mounted` guard prevents server/client hydration mismatch\n- [ ] Placeholder `<div>` matches button size (no layout shift on mount)\n- [ ] `aria-label` set for accessibility\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: dark mode default via next-themes + Tailwind dark: classes. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- `'use client'` required — `useTheme` is a client hook\n- `mounted` guard is MANDATORY: without it, SSR renders one icon and client\n  hydrates a different one → React hydration error\n- `Button` variant=\"ghost\" with `text-white hover:bg-white/10` blends with\n  the glassmorphism dark background\n- Component depends on `ThemeProvider` existing in layout (bead 8)","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:05:05.030299095Z","created_by":"quypq","updated_at":"2026-02-27T04:38:16.364412560Z","closed_at":"2026-02-27T04:38:16.362418460Z","close_reason":"Created src/components/theme-toggle.tsx: client component with useTheme, mounted guard (returns null pre-mount to prevent hydration mismatch), Sun/Moon icons from lucide-react, shadcn Button ghost variant.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-3ql","depends_on_id":"hdkit-project-1-2vh","type":"blocks","created_at":"2026-02-27T04:05:05.030299095Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-3ql","depends_on_id":"hdkit-project-1-2xr","type":"blocks","created_at":"2026-02-27T04:05:05.030299095Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-8pt","title":"Build TodoForm component","description":"Build the add-todo form with title input, due date picker, keyboard shortcuts, and server action integration.\n\n## Steps\n1. Create `src/components/todo-form.tsx`:\n\n```tsx\n'use client'\nimport { useRef, useEffect, useTransition } from 'react'\nimport { Input } from '@/components/ui/input'\nimport { Button } from '@/components/ui/button'\nimport { createTodo } from '@/actions/todos'\nimport { Plus } from 'lucide-react'\n\nexport function TodoForm() {\n  const titleRef = useRef<HTMLInputElement>(null)\n  const dateRef  = useRef<HTMLInputElement>(null)\n  const [isPending, startTransition] = useTransition()\n\n  // Keyboard shortcut: 'n' to focus title input\n  useEffect(() => {\n    const handler = (e: KeyboardEvent) => {\n      const target = e.target as HTMLElement\n      if (e.key === 'n' && target.tagName !== 'INPUT' && target.tagName !== 'TEXTAREA') {\n        e.preventDefault()\n        titleRef.current?.focus()\n      }\n    }\n    document.addEventListener('keydown', handler)\n    return () => document.removeEventListener('keydown', handler)\n  }, [])\n\n  function handleSubmit(e: React.FormEvent) {\n    e.preventDefault()\n    const title   = titleRef.current?.value.trim()\n    const dueDate = dateRef.current?.value || undefined\n    if (!title) return\n    startTransition(async () => {\n      await createTodo(title, dueDate)\n      if (titleRef.current) titleRef.current.value = ''\n      if (dateRef.current)  dateRef.current.value  = ''\n    })\n  }\n\n  return (\n    <form onSubmit={handleSubmit} className=\"flex gap-2 mb-4\">\n      <Input\n        ref={titleRef}\n        placeholder=\"Add a task… (press n)\"\n        disabled={isPending}\n        className=\"flex-1 bg-white/10 border-white/20 text-white placeholder:text-white/40\"\n        onKeyDown={e => e.key === 'Escape' && titleRef.current?.blur()}\n      />\n      <input ref={dateRef} type=\"date\" className=\"text-sm bg-white/10 border border-white/20 rounded-md px-2 text-white\" />\n      <Button type=\"submit\" size=\"icon\" disabled={isPending} className=\"bg-white/20 hover:bg-white/30\">\n        <Plus className=\"h-4 w-4\" />\n      </Button>\n    </form>\n  )\n}\n```\n\n## Acceptance Criteria\n- [ ] Pressing `n` (when not in an input) focuses the title field\n- [ ] `Enter` submits the form\n- [ ] `Esc` blurs the title field\n- [ ] Form resets after successful submit\n- [ ] `useTransition` wraps the server action call (no blocking UI)\n- [ ] Due date field is optional (empty = no due date)\n- [ ] Disabled state shown while server action is pending\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: glassmorphism-style input (bg-white/10, border-white/20), dark mode. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- `useTransition` is preferred over `useState(loading)` for Server Action pending state\n- Guard `n` shortcut: only fire when `event.target.tagName !== 'INPUT'`\n- `type=\"date\"` naturally produces `YYYY-MM-DD` format — no parsing needed\n- `useRef` for inputs avoids controlled state re-renders on every keystroke\n- Clean up the `keydown` event listener in the `useEffect` return","status":"closed","priority":1,"issue_type":"task","created_at":"2026-02-27T04:06:48.395099052Z","created_by":"quypq","updated_at":"2026-02-27T04:37:26.965220039Z","closed_at":"2026-02-27T04:37:26.963796339Z","close_reason":"Created TodoForm client component with Input+date picker, createTodo server action on submit, keyboard shortcuts: n to focus, Enter to submit, Esc to clear","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-8pt","depends_on_id":"hdkit-project-1-1xj","type":"blocks","created_at":"2026-02-27T04:06:48.395099052Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-8pt","depends_on_id":"hdkit-project-1-263","type":"blocks","created_at":"2026-02-27T04:06:48.395099052Z","created_by":"quypq"},{"issue_id":"hdkit-project-1-8pt","depends_on_id":"hdkit-project-1-2xr","type":"blocks","created_at":"2026-02-27T04:06:48.395099052Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-buy","title":"Replace empty state in todo-list.tsx with ClipboardList icon and styled copy","description":"## Context\n\nThe current empty state in TodoList is just a plain centered text div. This bead replaces it with a more polished version using a ClipboardList icon and improved typography.\n\n## What to Do\n\nEdit `src/components/todo-list.tsx`:\n\n**1. Add `ClipboardList` to imports (add a lucide-react import line):**\n```tsx\nimport { ClipboardList } from 'lucide-react'\n```\n\n**2. Replace the empty state `<div>`:**\n```tsx\n// BEFORE\nif (filteredTodos.length === 0) {\n  return (\n    <div className=\"py-8 text-center text-sm text-muted-foreground\">\n      {filter === 'completed'\n        ? 'No completed tasks yet.'\n        : filter === 'active'\n        ? 'All caught up!'\n        : 'No todos yet — add one above!'}\n    </div>\n  )\n}\n\n// AFTER\nif (filteredTodos.length === 0) {\n  return (\n    <div className=\"py-12 flex flex-col items-center gap-3 text-center\">\n      <ClipboardList\n        className=\"size-10 text-muted-foreground/40\"\n        strokeWidth={1.5}\n        aria-hidden=\"true\"\n      />\n      <div>\n        <p className=\"text-sm font-medium text-muted-foreground\">\n          {filter === 'completed'\n            ? 'No completed tasks yet.'\n            : filter === 'active'\n            ? 'All caught up!'\n            : 'No todos yet'}\n        </p>\n        {filter === 'all' && (\n          <p className=\"text-xs text-muted-foreground/60 mt-0.5\">\n            Add one above to get started.\n          </p>\n        )}\n      </div>\n    </div>\n  )\n}\n```\n\n## Technical Notes\n\n- No Tailwind v4 caveats here — all classes are standard utilities\n- `strokeWidth={1.5}` on the SVG makes the icon feel lighter and more decorative at 40px\n- `text-muted-foreground/40` uses Tailwind v4 opacity modifier syntax (supported natively)\n- The `LazyMotion` + `AnimatePresence` wrapper in the `return` statement is UNCHANGED\n- Filter logic is UNCHANGED\n\n## UI Standard\n\n- Pattern: empty state\n- Standard: inline — ClipboardList icon (size-10, muted/40, strokeWidth 1.5) + primary message + conditional sub-text for 'all' filter\n\n## Acceptance Criteria\n\n- [ ] Empty state renders ClipboardList icon above the message text\n- [ ] Messages unchanged: 'No completed tasks yet.' / 'All caught up!' / 'No todos yet'\n- [ ] 'all' filter shows sub-text 'Add one above to get started.'\n- [ ] Layout is centered column with py-12\n- [ ] LazyMotion/AnimatePresence and filter logic unchanged\n- [ ] `npm run typecheck` passes","status":"closed","priority":3,"issue_type":"task","created_at":"2026-02-27T08:40:23.540660643Z","created_by":"quypq","updated_at":"2026-02-27T09:00:56.294640659Z","closed_at":"2026-02-27T09:00:56.294370460Z","close_reason":"Added ClipboardList empty state with filter-aware copy and Framer Motion fade animation — implemented by SilverMountain","source_repo":".","compaction_level":0,"original_size":0}
{"id":"hdkit-project-1-f6o","title":"Add Framer Motion microinteractions to TodoItem","description":"Layer Framer Motion animations onto the functional TodoItem component for polished microinteractions.\n\n## Steps\n1. Install: `npm install framer-motion`\n2. Update `src/components/todo-list.tsx` to ensure `AnimatePresence` is imported from `framer-motion`\n3. Update `src/components/todo-item.tsx` to use optimized `m` components with `LazyMotion`:\n\n```tsx\n// In todo-list.tsx — wrap list with LazyMotion for bundle optimization\nimport { LazyMotion, domAnimation, AnimatePresence } from 'framer-motion'\n\n// Wrap <ul> with LazyMotion:\n<LazyMotion features={domAnimation}>\n  <AnimatePresence initial={false}>\n    {filtered.map(todo => <TodoItem key={todo.id} todo={todo} />)}\n  </AnimatePresence>\n</LazyMotion>\n\n// In todo-item.tsx — replace <li> with motion.li (or m.li)\nimport { m } from 'framer-motion'\n\n// Replace <li> with:\n<m.li\n  layout\n  initial={{ opacity: 0, y: -10 }}\n  animate={{ opacity: 1, y: 0 }}\n  exit={{ opacity: 0, x: -20, transition: { duration: 0.2 } }}\n  transition={{ duration: 0.2 }}\n  // ... existing className and handlers\n>\n\n// Animate checkbox bounce via whileTap on Checkbox wrapper:\n<m.div whileTap={{ scale: 1.3 }} transition={{ type: 'spring', stiffness: 400 }}>\n  <Checkbox ... />\n</m.div>\n\n// Animate title strike-through (use framer-motion layout transition):\n<m.span\n  layout\n  className={`flex-1 text-sm text-white transition-opacity ${todo.completed ? 'line-through opacity-50' : ''}`}\n>\n  {todo.title}\n</m.span>\n```\n\n## Acceptance Criteria\n- [ ] New todo items animate in (fade + slide down)\n- [ ] Deleted todo items slide out left + fade (via `AnimatePresence` + `exit`)\n- [ ] Checkbox tap has a spring scale bounce\n- [ ] Completed title transitions smoothly to strike-through\n- [ ] `LazyMotion` + `domAnimation` used (NOT full `motion` import) — bundle ~4.6kb not ~34kb\n- [ ] Animations don't block interaction (`layout: false` on non-layout variants)\n- [ ] No layout shift after animations complete\n\n## Technical Notes\n### UI Standard\nFollow brainstorm.md UI/UX decisions: Framer Motion microinteractions — checkbox scale bounce, strike-through on complete, slide-out on delete via AnimatePresence. Reference: history/20260227-todo-app-nextjs16/brainstorm.md\n\n- IMPORTANT: Use `m` + `LazyMotion` pattern, NOT `motion.li` directly — this\n  keeps the feature JS to ~4.6kb vs the full ~34kb motion bundle\n- `LazyMotion` provider belongs in `todo-list.tsx` wrapping `AnimatePresence`,\n  NOT in `layout.tsx` — scoped to where animations are used\n- `layout` prop on `m.li` enables smooth reordering when filter changes\n- `initial={false}` on `AnimatePresence` prevents entrance animations on page load\n- Avoid `layoutId` unless implementing shared element transitions (out of scope)","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T04:07:37.286745258Z","created_by":"quypq","updated_at":"2026-02-27T04:40:32.336902267Z","closed_at":"2026-02-27T04:40:32.336476167Z","close_reason":"Added Framer Motion microinteractions using LazyMotion+domAnimation+m components: slide-in/out with AnimatePresence in TodoList, checkbox bounce on toggle, animated strikethrough on complete, slide-out on delete","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-f6o","depends_on_id":"hdkit-project-1-2rb","type":"blocks","created_at":"2026-02-27T04:07:37.286745258Z","created_by":"quypq"}]}
{"id":"hdkit-project-1-y1e","title":"Harmonize TodoItem with glass-surface and replace inline SVG with Trash2","description":"## Context\n\nTodoItem currently uses `bg-card` (a solid opaque background) inside the glassmorphism container, breaking the depth illusion. The delete button also uses an inline SVG instead of the lucide-react `Trash2` icon used elsewhere.\n\n## What to Do\n\nEdit `src/components/todo-item.tsx`:\n\n**1. Replace `bg-card` with `glass-surface` in the `m.li` className:**\n\n```tsx\n// BEFORE (in m.li className)\n\"... rounded-lg border bg-card px-4 py-3 shadow-xs ...\"\n\n// AFTER\n\"... rounded-lg glass-surface border-transparent px-4 py-3 shadow-xs ...\"\n```\n\nThe full `m.li` className becomes:\n```tsx\nclassName=\"flex items-center gap-3 rounded-lg glass-surface border-transparent px-4 py-3 shadow-xs transition-colors hover:bg-accent/5 focus-within:ring-1 focus-within:ring-ring/30 outline-none list-none\"\n```\n\n**2. Add `Trash2` to the lucide-react import:**\n```tsx\n// BEFORE\nimport { Pencil, Check, X } from 'lucide-react'\n\n// AFTER\nimport { Pencil, Check, X, Trash2 } from 'lucide-react'\n```\n\n**3. Replace the entire inline SVG in the delete button with:**\n```tsx\n<Trash2 size={14} aria-hidden=\"true\" />\n```\n\n## Visual Notes\n\n- `glass-surface` is defined in globals.css: `background: var(--glass-bg-strong); backdrop-filter: blur(calc(var(--glass-blur) / 2)); border: 1px solid var(--glass-border);`\n- In light mode: `var(--glass-bg-strong)` = `oklch(1 0 0 / 80%)` — semi-transparent white. If this looks washed out against the violet gradient, fallback to `bg-white/5 border-white/10`\n- In dark mode: `var(--glass-bg-strong)` = `oklch(1 0 0 / 14%)` — subtle translucent layer\n- `border-transparent` overrides the `border` from glass-surface to remove the visible border (keeping backdrop filter but removing the stroke)\n\n## Technical Notes\n\n- All existing Framer Motion animations (checkbox bounce, strikethrough scaleX, delete exit) are UNAFFECTED — only className changes\n- Tailwind v4 CSS-first: `glass-surface` is a custom @layer utility, already defined in globals.css\n- Do NOT import anything new except `Trash2`\n\n## UI Standard\n\n- Pattern: list view (todo items)\n- Standard: inline — glass-surface items inside glassmorphism container for visual depth continuity\n\n## Acceptance Criteria\n\n- [ ] `m.li` uses `glass-surface border-transparent` (not `bg-card border`)\n- [ ] Delete button renders `<Trash2 size={14} />` (no inline SVG)\n- [ ] `Trash2` added to lucide-react import\n- [ ] All animations (bounce, strikethrough, delete exit) still work\n- [ ] In dark mode: items appear semi-transparent against the container\n- [ ] `npm run typecheck` passes","status":"closed","priority":2,"issue_type":"task","created_at":"2026-02-27T08:38:45.049617652Z","created_by":"quypq","updated_at":"2026-02-27T08:56:29.029783Z","closed_at":"2026-02-27T08:56:29.029783Z","close_reason":"Applied glass-surface styling to TodoItem m.li (replaced border bg-card with glass-surface border-transparent), added Trash2 to lucide-react import, replaced inline SVG delete icon with Trash2 component. All Framer Motion animations preserved.","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"hdkit-project-1-y1e","depends_on_id":"hdkit-project-1-2lj","type":"blocks","created_at":"2026-02-27T08:42:36.005933322Z","created_by":"quypq","metadata":"{}","thread_id":""}]}
